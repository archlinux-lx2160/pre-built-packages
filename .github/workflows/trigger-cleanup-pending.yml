name: Cleanup stale pending releases

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cleanup:
    uses: tehrengruber/arch-pre-built-packages/.github/workflows/cleanup-pending.yml@v0.1
