name: Run in Arch Linux ARM Docker
description: Load or build cached Arch Linux ARM image and run commands inside it

inputs:
  run:
    description: "Commands to run inside the container"
    required: true
  mount:
    description: "Volumes to mount in host:container format (one per line)"
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Cache Docker image
      uses: actions/cache@v5
      id: docker-cache
      with:
        path: /tmp/archlinux-aarch64.tar.zst
        key: archlinux-aarch64-v1

    - name: Build Arch Linux ARM Docker image
      if: steps.docker-cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        curl -LO http://os.archlinuxarm.org/os/ArchLinuxARM-aarch64-latest.tar.gz
        cat <<'DOCKERFILE' > /tmp/Dockerfile.archlinux
        FROM scratch AS base
        ADD ArchLinuxARM-aarch64-latest.tar.gz /
        RUN pacman-key --init && pacman-key --populate archlinuxarm
        RUN sed -i '/^\[options\]/a DisableSandbox' /etc/pacman.conf
        RUN pacman -Rdd --noconfirm $(pacman -Qq | grep '^linux') 2>/dev/null || true
        RUN pacman -Syu --noconfirm base-devel git
        RUN sed -i '0,/if (( EUID == 0 ))/s/if (( EUID == 0 ))/if false/' /usr/bin/makepkg
        RUN pacman -Scc --noconfirm && rm -rf /var/log/* /tmp/* /var/cache/pacman/pkg/*
        FROM scratch
        COPY --from=base / /
        DOCKERFILE
        docker build -f /tmp/Dockerfile.archlinux -t archlinux-aarch64 .
        docker images archlinux-aarch64
        docker save archlinux-aarch64 | zstd -T0 -o /tmp/archlinux-aarch64.tar.zst
        ls -lh /tmp/archlinux-aarch64.tar.zst

    - name: Import Docker image
      if: steps.docker-cache.outputs.cache-hit == 'true'
      shell: bash
      run: zstd -d /tmp/archlinux-aarch64.tar.zst --stdout | docker load

    - name: Write script
      shell: bash
      run: |
        cat > /tmp/archlinux-docker-run.sh << 'ARCHLINUX_DOCKER_SCRIPT_EOF'
        ${{ inputs.run }}
        ARCHLINUX_DOCKER_SCRIPT_EOF
        chmod +x /tmp/archlinux-docker-run.sh

    - name: Run in container
      shell: bash
      run: |
        VOLUME_ARGS=""
        while IFS= read -r line; do
          [ -z "$line" ] && continue
          VOLUME_ARGS="$VOLUME_ARGS -v $line"
        done <<< "${{ inputs.mount }}"
        docker run --rm $VOLUME_ARGS \
          -v /tmp/archlinux-docker-run.sh:/tmp/archlinux-docker-run.sh:ro \
          archlinux-aarch64 \
          bash /tmp/archlinux-docker-run.sh